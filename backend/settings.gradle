rootProject.name = 'internal-java-be-modules'
include(
        "modules:identity",
        "modules:_common",
        "modules:ai",
        "modules:document",
        "modules:ocr",
        "modules:payment",
        "modules:transaction",
        "modules:wallet"
)
